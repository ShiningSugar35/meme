# 进度验收

## 2026-09-03：Trending 缺失事实补齐、生命周期前置与 missing-only 回溯

状态：`COMPLETED / HOT-UPDATED / NEW 24H SHADOW WINDOW ACTIVE`

- 根因复核：Trending 与 Trenches 在 prefilter 后实际共用同一 `EnrichmentService.enrich()`；二者都会拉 `token_info + token/security + token/pool_info`。差异是 `/v1/trenches` raw row 的 `insider_ratio` 覆盖100%，而 `/v1/market/rank` raw 为0%；真实 `/v1/token/security` 契约探针虽返回 success 和 taxes/burn/renounced/top10 等字段，但当前生产 OpenAPI 响应不返回任何 insider 字段，因此旧实验所有 Trending deep enrichment 都确定性死在 `missing_or_invalid:insider_ratio`。
- 生命周期：GMGN Trending 官方没有 Trenches 的 `type=new_creation/near_completion` 参数；生产前置改用官方 `is_internal_market`，表达仍在 launchpad 内部、尚未 migrated/completed 的 `new_creation + near_completion` 并集。新实验在线 raw `launchpad_status` 当前为0（81/81，100% internal），旧 status=1 已不再返回。
- Insider 严格边界：GMGN range 是闭区间，本地合同是 `insider_ratio < 0.2`，因此新请求使用 `math.nextafter(0.2,-inf)=0.19999999999999998`。只有带 `source=gmgn_market_rank + predicate=lt + declared_limit<=0.2 + actual_request_limit<0.2` 的 Trending provenance 才能在数值仍缺失时满足该 required fact；绝不写 `insider_ratio=0` 或其他假值，Trenches 不能使用此机制绕过缺失字段。
- Enrichment 重构：首次完整 `token_info + security + pool` bundle 只拉一次；required-facts 缺失时先合并同一 cycle 已观察到的 Trenches raw（仅填缺值，不覆盖 Trending 的当前 price/entry fact），再按字段映射只重拉可能提供该字段的 endpoint family；实际字段仍补不到时才允许 insider 的严格 server qualification fallback。私有 `_server_qualified_facts` 只原样嵌套保存，不扁平化污染业务 source；完整 SafetyFilter、top-holders、created_tokens、PIT Kline 继续与 Trenches 共用。
- 历史回溯冻结口径：旧正式实验 `disc24h_6eca0f7ef9a7` 中，仅纳入 prefilter 已通过、唯一 deep reject 为 `missing_or_invalid:insider_ratio`、`launchpad_status=0`、且观察时点+90m 已到期的候选；每个 `(source,address)` 仅取第一次可执行 observation，使用当时 rank price，并以1m K线执行现行90m first-touch TP1.8/SL0.9 标签。没有用未来安全字段反向挑样本。
- 历史回溯完成度：截至本轮所有候选均已满90m，冻结集合共164 unique 地址，Kline 164/164 成功、error=0、not_due=0。已完成的141个历史地址直接复用此前落盘的Kline标签结果，仅补拉新增成熟地址，避免重复API；一次性回标限速最终为0.30 weighted units/s，不借用0/1/2号 production discovery key。
- missing-only 反事实最终结果：`volume` 159笔/24正类=`15.09%`，Wilson95 `[10.36%,21.48%]`，+3/-1 units=`-63`、J=`-2.625`；`smart_degen_count` 157/26=`16.56%`，Wilson95 `[11.56%,23.16%]`，units=`-53`、J=`-2.038`；`change5m` 160/21=`13.13%`，Wilson95 `[8.75%,19.23%]`，units=`-76`、J=`-3.619`；按地址取三来源最早 entry 的 union 为164/25=`15.24%`，Wilson95 `[10.54%,21.54%]`，units=`-64`、每笔期望 `-0.390 unit`、J=`-2.56`。同窗生产标签最终对照：`new_creation` 55/12=`21.82%`、units=`-7`、每笔`-0.127`；`near_completion` 108/14=`12.96%`、units=`-52`、每笔`-0.481`。
- 回溯边界：这是用户指定的“仅因缺失 insider 就视为通过”的反事实筛查；旧链在 insider required-fact 前终止，当时的 top-holder 硬门从未执行且无法 PIT 重构，因此上述15.24%不能冒充完整 SafetyFilter 最终准入率。但它足以证明：字段 Bug 解释了旧0 accepted，同时“把 missing-only 全部放行”本身仍明显低于25%盈亏平衡。
- 新在线实验：旧 `disc24h_6eca0f7ef9a7` 已 cancelled 并保留审计；修正后窗口为 `disc24h_c37f12126a13`。截至最新验收，Trending 三来源各已有32个 accepted sample、17个 mature；volume 2/17正类=`11.76%`，smart/change 各1/17=`5.88%`。`missing_or_invalid:insider_ratio` 已不再作为阻断，后续 reject 来自真实完整 SafetyFilter/top-holder 等规则。该在线样本仍很小，只证明管线恢复，不能替代历史164笔反事实统计。
- API/运行态：验收观测时新窗口 `/v1/market/rank` 已369 requests、0 rate-limit/0 failure；token info/pool/security 分别223/223/225，均0 failure；shared limiter 累计3493 weighted units。top-holders 因更多候选真正进入后置筛选累计146 requests，其中早先出现9次短暂429/failure；从134/9继续增长到146/9期间未再新增429，说明一次性历史Kline回标结束后 key pressure 已恢复。`/health=ok`、8000单 listener、`DRY_RUN=true`、simulation=true，真实 BUY 未开启。
- 测试：missing-facts/lifecycle/同周期Trenches补全相关定向最终21/21 PASS；全量 pytest **241/241 PASS**；compileall/py_compile PASS；最终 `git diff --check` PASS、SQLite `foreign_key_check=0`、diff high-confidence secret scan=0。
- 研究资产：最终回溯明细保存于 `artifacts/research/trending_missing_only_label_backfill_20260903.json`，可复现脚本为 `artifacts/research/trending_missing_only_label_backfill.py`。

## 2026-09-02：Trending 前置筛选修正与正式24小时窗口重开

状态：`CANCELLED / INVALID MISSING-INSIDER PIPELINE / PRESERVED FOR AUDIT`

- 根因：首轮 pilot 的 `/v1/market/rank` 仅传 `filters=renounced,frozen` 与 launchpad，age/liquidity/marketcap/holder/top10/insider/bundler 等原规则是在 Top80 返回后才本地粗筛，造成大量不可交易 Token 占满排序名额，三 Trending 来源粗筛存活率极低。
- 官方合同：GMGN Trending 支持 `min/max-created`、liquidity、marketcap、holder-count、top10-holder-rate、insider-rate、bundler-rate 等 server-side range；`--limit` 默认100、最大100。当前代码不再显式发送 Trending `limit`。
- 已前置但不放宽：`min_created=2m`、`max_created=300m`、`min_liquidity=4800`、`min_marketcap=5000`、holders `30..999`、top10 `0.14..0.25`、`max_insider_rate=0.2`、`max_bundler_rate=0.2`；GMGN 闭区间只负责缩小 universe，本地原 SafetyFilter 严格不等式及 rug/fresh-wallet/tax/sniper/比值/1h activity 等规则全部保留。
- 旧实验 `disc24h_2de1a448bde4` 于北京时间 2026-09-02 11:31 左右标记 `cancelled / invalid pilot`，数据完整保留但不得与新正式窗口拼接。
- 新正式实验 `disc24h_6eca0f7ef9a7`：北京时间 `2026-09-02 11:31:06` 至 `2026-09-03 11:31:06`，之后仅补齐尾部90分钟标签。Trending 使用 GMGN server default/max 100；生产 Trenches 仍受其官方 endpoint max80 约束，二者不再共用同一 limit。
- 负载重新按最近120个生产周期计算：进入 enrichment 平均约1.08、P95=3，生产P95约42 weighted units/cycle；全局仍保持2 weighted units/s、70% Collector budget，新的 shadow deep-enrich cap=10 unique/cycle，仍预留30%共享容量。
- 热更新后前两轮正式数据：三个 Trending 来源各累计13 observations / 7 unique，其中各6条通过本地粗筛；相较旧 pilot 每周期固定80条且粗筛存活率约0.6%~1.5%，说明 server-side universe 前置已实际改变返回集合。当前 `/v1/market/rank`、enrichment 与共享 limiter 均为0个429；API failure=0。三路尚无 accepted shadow sample，是后置 enrichment/SafetyFilter 继续拒绝，不是 Collector 停摆。

## 2026-09-02：24小时 Trending 三池 vs Trenches 对照实验

状态：`CANCELLED / INVALID PILOT / PRESERVED FOR AUDIT`

### 用户裁决与实验窗口

- 未来24小时并行观察现有 Trenches control 与 `volume`、`smart_degen_count`、`change5m` 三个 GMGN Trending treatment；统一记录候选、准入、标签、winner、J、重合和 API 负载。
- 实验 ID：`disc24h_2de1a448bde4`。采样窗口：北京时间 `2026-09-02 09:15:02` 至 `2026-09-03 09:15:02`。采样停止后不再新增候选，仅继续补齐最后90分钟样本的标签，待 pending=0 后自动 completed。
- control：`trenches:new_creation`、`trenches:near_completion`；treatment：`trending:volume`、`trending:smart_degen_count`、`trending:change5m`。Trending 固定 SOL、`interval=5m`、`limit=80`、`direction=desc`。

### 官方依据与负载预算

- GMGN 官方 gmgn-skills 的 market limiter 为 leaky-bucket rate/capacity `20/20`；Trending `/v1/market/rank` weight=1、Trenches `/v1/trenches` weight=3、Kline weight=2、Top holders weight=5；429 必须遵循 reset/cooldown。
- 本地 12 个 GMGN Key：slot0/1 为两类生产 discovery，slot2 为 discovery fallback，slot3..11 为 auxiliary；共享 limiter 继续保持 `2.0 weighted units/s`，本轮未提速。
- 最近120个生产周期：平均约2.16个候选进入高权重 enrichment，P95=5、max=7；按完整深筛最坏12 weight/地址，生产 P95 约66 weight/cycle。三条 Trending discovery 固定只新增3 weight/cycle。
- 以 120s 周期、70% Collector weighted capacity 计算：`2*120*0.70-66-3=99`，折算得到 Trending-only 深筛 cap=`floor(99/12)=8 unique/cycle`，约保留30%共享容量给 PositionMonitor、标签与其他 GMGN 流量。超额候选显式记 `budget_censored`。

### 实现与审查返工

- schema v14 新增 experiment/observation/shadow-sample/API-metric 四类附加账本，不修改正式 `samples`、模型、仓位或 simulation。
- Trenches 仍是正式生产入口；control 只通过 Collector observer 复制 source membership/outcome 和后续成熟标签。Trending treatment 永不进入正式 samples、训练、Prediction 或 rules_only。
- 三个 Trending source 先记录各自真实返回时刻和 rank，再统一 SafetyFilter prefilter；通过者按地址跨来源去重，同地址只在实际 Trending 发现之后执行一次深 enrichment，再扇出各来源 shadow sample。
- 独立审查发现不能复用同周期较早的 Trenches accepted entry-time 作为 Trending entry，因为会造成来源时点偏差；已删除跨 control/treatment 的 entry fact 复用。回归明确要求 Trending `entry_time >= source_observed_at`。
- 同地址多个 treatment 的90分钟标签按地址合并一次 Kline 请求；control 直接同步生产已成熟标签，不重复发 Kline。
- reserved discovery keys 按累计 route weight 选择最轻且未 cooling 的 slot；标准调用序列可得到 slot0/1/2=`3/3/3` weighted units。429 只冷却命中 slot。
- 生产 Trenches 若本周期进入 rate-limit circuit 或 network recovery，Trending shadow 整周期跳过并记录 `skipped_production_pressure`，实验流量不与生产恢复抢容量。
- API telemetry 只记录 path、route weight、slot index、latency、outcome/429，不记录 API key/Authorization；共享 limiter 额外记录每周期 total weighted delta。

### 指标与可观测性

`scripts/discovery_experiment_status.py` 持续输出：

- 每来源 observations、raw unique、prefilter pass、accepted、enrichment reject、budget-censored；
- mature/positive/positive-rate、+3/-1 profit units、`J_trade_all`；
- winner union/capture、与 Trenches 的全窗重合、同周期重合、Trenches 完全漏掉 winner、截至 treatment entry 时尚未被 Trenches 发现的 winner；
- API route requests/weighted units/429/failures/average latency，以及 Collector client 各 key 的累计 weighted load；`__shared_global__` 另表示整个共享 limiter 的系统级 weighted 消耗。

### 测试与发布门

- 定向 Collector/Trending/experiment 回归：19/19 PASS；专项 experiment 回归：5/5 PASS。
- 全量 pytest：236/236 PASS。
- Python `compileall backend tests scripts`：PASS。
- `git diff --check`：PASS。
- 高置信 secret scan：223 个 Git 源文件，0 命中。
- 生产 SQLite schema=`14`，`foreign_key_check=0`。

### 热更新与首轮运行证据

- `scripts/start_runtime.py --reload` 已安全拉起现有 runtime；8000 端口只存在一个 listener。`/health=ok`，`DRY_RUN=true`、simulation=true、live trading=false。
- 首个完整 Collector cycle 持续约87.95s，无 transport/network/rate-limit 错误；生产 Trenches 返回120个候选，正式 SafetyFilter 行为保持不变。
- 首轮五来源都已实际落账：Trenches new_creation=60、near_completion=60；Trending volume=80、smart_degen_count=80、change5m=80。首轮 raw overlap：volume 与 Trenches 13、smart_degen 1、change5m 10。当前尚无成熟 experiment label，不能据此判断来源优劣。
- 首轮 API 账本无429、无failure；`/v1/trenches`=6 weighted units；`/v1/market/rank`累计4 requests/4 weight（3个 treatment + 1个既有 market-regime 查询）；shared global 本周期实际83 weighted units，低于本轮 Collector 预算168。reserved discovery slot0/1/2 各为3 weighted units；auxiliary slots3..11实际分布约5~12 weighted units。后续约1.5小时持续观测仍为0个429/0 failure，reserved slot0/1/2 达到138/138/138，Collector auxiliary 约105~143；共享 limiter 累计1566 weighted units。
- `rules_only`、Collector、Prediction、TrainingWorker、Scheduler、PositionMonitor 均保持运行；实验失败路径与生产 circuit 隔离。

### 当前结论边界与回滚

- 目前只能确认**实验与负载均衡已生效**，不能提前宣称 Trending 比 Trenches 更优。最终来源比较必须等24小时采样结束并补齐尾部90分钟标签后再读取同一账本。
- 回滚只需停用/结束 discovery experiment 并恢复上一提交；新增表均为附加账本。正式样本、标签、模型、仓位、rules_only 与 `.env` 未被改写。

## 2026-08-31：研发工作流提速与自动收尾规范优化

状态：`COMPLETED / DOCS-ONLY / NO RUNTIME RESTART`

- 根因：旧流程把文档/规范任务与量化模型、数据库、前端、热更新任务共用同一 13 步强制链，导致低风险变更也机械执行权威研究、全量测试、前端构建和运行验收；同时没有把本任务临时文件清理定义成终局硬门。
- 实现：`AGENTS.md` 与 `开发文档.md` 将执行视图压缩为 5 个门：范围/根因与必要研究 → 架构与实施方案 → 实现 + defect-first Review + 分级验证 → 按风险发布/上线 → 文档归档 + 临时文件清理 + commit/push。
- 验证分级：L0 文档/流程只做相关文档/文本检查、必要静态检查和 `git diff --check`；L1 单模块低风险代码做 targeted + 受影响 lint/typecheck；L2 跨模块/运行态增加相关集成与 smoke；L3 标签/特征/时间切分、模型认证、资金/账本、实盘权限、安全/迁移/发布继续保留完整专业门。
- 清理边界：只允许删除本任务生成且可证明可再生的临时脚本、一次性日志/XML、临时 build/smoke 目录和缓存；`.env`、`data/meme_quant.db*`、`ml_models/`、正式 `artifacts/`、生产日志/审计账本、备份、未知来源文件和用户既有未提交文件均禁止自动清理。
- 安全不变：`DRY_RUN=true`、时间序列/未来函数、final holdout、部署认证和真实交易权限规则没有降低。
- 本任务属于 L0 文档/流程变更，因此不执行训练、前端 build、数据库迁移或热更新；验收只使用文档 Review、`git diff --check` 与 worktree 审计。

本文只记录变更过程和验收事实，不承担长期架构规范。最新任务置顶；每个条目必须给出需求、根因、研究、实现、审查、测试、部署、回滚点和未决风险。

## 2026-08-31：Phase19 Precision-Recall 期望收益目标重构

状态：`COMPLETED / HOT-UPDATED / CURRENT GENERATION FINAL-VETOED`

### 用户裁决与数学目标

- 目标函数直接贴近 Precision、Recall 与当前盈亏比，不再叠加尺度不一致的复合权重。
- 当前标签代理收益为 Winner `+3` 单位、Loser `-1` 单位。设 Precision=`P`、Recall=`R`、收益/损失比 `b=3`，正类数为 `N+`：`TP=R·N+`，`FP=TP·(1/P-1)`，因此 `U=b·TP-FP=N+·R[(b+1)-1/P]`。
- 唯一主目标定义为 `J(P,R,b)=U/N+=R[(b+1)-1/P]`；当前 `b=3` 时 `J=R(4-1/P)=4R(1-0.25/P)=(3TP-FP)/N+`。盈亏平衡 Precision 为 `P*=1/(b+1)=25%`。
- `J>0` 与 `3TP-FP>0` 完全等价；Precision 提高单笔期望，Recall 扩大正期望机会捕获，因此不再额外人为给 Recall 加权。

### 根因

- Phase18 `S=0.60E+0.40G` 存在严重尺度失真：GradientBoosting `E≈0.0149`、`G≈0.4178`，最终 S 约 95% 来自 G；`G=0.60 AP_skill+0.20 stability+0.20 decay` 即使 AP skill 接近 0，只要稳定且无衰减也可得到接近 0.4 的正基线。
- Phase18 阈值只允许 5%/7.5%/10%/15% 四个 sparse budget；当代全部成功候选都选择 5%。冻结绝对阈值后 final 实际入选率降到约 0.5%~1.25%，GradientBoosting/CatBoost Recall 均仅 2.47%，ExtraTrees 为 0%。
- ExtraTrees development 仅 `17TP/50FP`、`U=+1`，仍因 G 高基线进入 Top3，证明旧复合目标会放大“稳定但经济优势极弱”的模型。

### 权威依据与冻结方案

- scikit-learn Precision-Recall 文档把每个阈值对应的 `(Recall, Precision)` 定义为 operating point；cost-sensitive threshold tuning 示例使用业务 gain/cost scorer 搜索阈值空间中的最优 cut-off，而不是先限制固定几个百分位。
- development OOS calibration 继续保持时间顺序；production threshold 搜索全部 distinct calibrated-probability operating points，`min_trades` 只作最小统计下限。
- operating point 直接最大化 J；J 并列时依次优先 Recall、Precision、Wilson 下界。
- Top3 只按 development OOS J 排名；feature-count Occam 也只围绕同一个 J 做 one-SE/不超过 8% 相对损失控制。AP/AUC、stability、decay、execution PnL 和 family diversity 全部降为 audit-only。
- final 只用冻结 threshold 计算 P/R/J 并作 veto；support + selected>=1 + `J>0` 才取得逐模型 paper 资格，final 不参与调阈值、换 Top3 或重选特征。

### 实现与独立审查

- 经济目标升级为 `precision_recall_payoff_v4`；decision/certification 升级为 `phase19_expected_return_paper_v1` / `phase19_expected_return_certification_v1`，旧 Phase18 工件自动 stale。
- 新增 canonical `expected_return_score_from_counts=(3TP-FP)/N+` 与等价 P/R 公式；25% break-even 由 +3/-1 比例直接计算。
- `select_sparse_budget` 名称/工件字段仅保留兼容，production 行为已改为完整 operating-point 搜索。实现采用一次降序排序 + 累积 TP/FP，复杂度 `O(N log N)`，不会因完整阈值空间退化到 `O(N²)`。历史 5%/7.5%/10%/15% fractions 仅在显式 replay/test override 时保留。
- Trainer 删除 `economic_weight/generalization_weight` 和 `0.60E+0.40G`；`economic_score=ranking_economic_score=composite_score=J`。Top3 直接取 J 前三，不再为了模型家族多样性牺牲目标值。
- Final certification 统一记录 `final_expected_return_score`；AP/AP lift/ROC-AUC 只写 `final_ranking_diagnostic_pass`，不作为 blocker。单个 slot final J 非正只让该 slot shadow，不拖死其他合格 slot。
- 前端 Dashboard/Models 删除 E/G/S 展示，直接展示 Precision、Recall、理论收益和期望收益 J，并显示 `J=Recall×(4-1/Precision)` 与 25% break-even 公式。
- 前端对历史模型按 `economic_objective_version` 做口径隔离：旧 Phase18 Active slots 显示“合同已失效/历史口径”，不会把历史综合分 S 冒充为 J；训练记录中的 `blocked_certification` 明确显示“认证否决”而非“待切换”。
- 独立审查确认 threshold/Top3 只读取 chronological development OOS；final fitted estimator 与 production estimator 相同，final 标签不进入 refit。

### 测试与发布门

- Phase19 核心/审查回归：50/50 PASS。
- 全量 pytest：228/228 PASS。
- Python `compileall backend tests scripts artifacts/research`：PASS。
- 前端 `tsc -b && vite build`：PASS，2183 modules transformed。
- `git diff --check`：PASS。
- 高置信 secret scan：219 个 Git 源文件，0 命中。
- SQLite `foreign_key_check`：0 违规；最近 12 个模型工件按生产 import path 12/12 加载成功。

### 真实 Phase19 重训与 final 结果

真实 run：`f967fa49-fe27-4465-a648-458ad442af62`。新的 Top3 完全按 development J 产生：LightGBM / XGBoost / ExtraTrees。

| 模型 | Development operating point | Development J | Frozen final | Final J | 结果 |
|---|---|---:|---|---:|---|
| LightGBM `b25080c2` | 108/1342=8.05%，P=28.70%，R=16.49%，31TP/77FP | +0.0851 | 23 笔，P=17.39%，R=5.06%，4TP/19FP | -0.0886 | shadow |
| XGBoost `937ac560` | 12/1342=0.89%，P=58.33%，R=3.72%，7TP/5FP | +0.0851 | 0 笔，P=0，R=0 | 0 | shadow |
| ExtraTrees `fee34ad5` | 17/1342=1.27%，P=47.06%，R=4.26%，8TP/9FP | +0.0798 | 1 笔，0TP/1FP | -0.0127 | shadow |

- LightGBM 的最优 development selection 自然扩大到 8.05%、Recall 16.49%，证明旧 5% budget 对 Recall 的硬限制已被删除。XGBoost/ExtraTrees 的最优点仍较稀疏，是同一 J 公式在 development OOS 上自然得到的结果，而不是新的固定 Recall/交易频率门。
- frozen final 共 565 条、79 正类。三模型 final J 均不大于 0，因此 generation `eligible=false`、`promoted=0`，blocker=`no_top3_model_has_positive_expected_return_final_evidence`。没有为了热更新强推模型，也没有看完 final 后反向改公式。
- 部署一致性：三模型 production/evaluation frozen-final 最大概率差均为 0.0；training→final gap=5626 秒，超过 90 分钟标签窗口；decision/cert/fit-scope/age/gap 检查全部 PASS。

### 热更新与运行验收

- 现有 Windows supervisor 已热加载 Phase19；`start_runtime.py --reload` 幂等返回 `already_running`，没有启动第二个 Uvicorn。
- `/health=ok`；Collector、Prediction、TrainingWorker、Scheduler、PositionMonitor 均 running。
- 当前新 Phase19 LightGBM/XGBoost/ExtraTrees 被 Prediction 作为 shadow generation 加载；`tradable_model_ids=[]`。旧 Phase18 slots 因新 economic/decision contract stale 而不允许继续开模型仓。
- `rules_only` 已持续到 530 笔已平仓，没有被模型 final veto 或换代阻断。
- `DRY_RUN=true`、`simulation_enabled=true`、`live_trading_enabled=false`。

### 回滚点与技术债

- 未改写历史样本、标签、仓位、交易、risk 输出或旧模型工件；新目标通过版本合同隔离。
- 当前没有 tradable 模型是本次独立 final 对新目标的真实否决结果，不是 pipeline 停摆。后续只能等待新 OOS 数据和下一次训练，不得使用已经观察的本次 final 反向修改阈值公式。
- scikit-learn `SVC(probability=True)` 仍有 1.11 移除警告，后续独立迁移。

### 开发进度

- [x] 恢复生产/Git/模型事实并量化根因。
- [x] 数学公式与架构方向冻结。
- [x] 代码实现与定向回归。
- [x] 独立审查与返工。
- [x] 全量测试与真实重训。
- [x] 热更新和运行验收。
- [x] commit/push。

## 2026-08-31：Phase18 execution-risk 从 paper/认证硬门降级为 shadow

状态：`COMPLETED / DEPLOYED`

### 用户裁决

- 删除 execution-risk 固定 0.40 作为 `model_1/2/3` paper 开仓硬门。
- 删除 execution-risk 作为 deployment certification / generation activation 的硬门；满足规则准入、主模型冻结阈值和 final 基础模型证据的模型可上线模拟交易。
- execution-risk 模型、历史输出和 outcome 继续保存，只作为 shadow 研究、监控和后续模型设计依据。
- 完成安全热更新；保持 `DRY_RUN=true`、真实自动 BUY 关闭。

### 根因与研究依据

- Phase17 run `905c3aed-e1a5-4ba8-becb-3949912bf102` 中，GradientBoosting frozen-final 基础模型为 7 笔、3TP/4FP、`3TP-FP=+5`，但固定 `execution-risk<=0.40` 后只剩 2 笔且 0TP/2FP、`3TP-FP=-2`。固定阈值在该独立窗口把正向主模型证据反向筛成负证据。
- scikit-learn decision-threshold 文档明确区分概率估计与行动阈值：阈值应与明确业务效用/验证目标绑定，而不是把任意概率常数当成普适决策边界；概率校准本身也不定义交易效用阈值。
- Federal Reserve 2026 model-risk guidance 强调 outcome analysis、ongoing monitoring，以及表现偏离后调整/再校准/重建；overlay 和限制应有与用途相匹配的验证证据。
- 本轮没有用上述 final 结果寻找新的 risk 阈值，而是删除未经独立经济效用证明的第二分类器硬否决，让 risk 从未来 OOS outcome 中继续 shadow 研究。

### 实现与独立审查

- 决策合同升级为 `phase18_model_filter_paper_v1`，认证合同升级为 `phase18_model_filter_certification_v1`；旧 Phase17 工件自动 fail-closed。
- Paper 主链固定为 `规则安全准入 → calibrated probability >= model development OOS threshold → simulation`。execution-risk 和 drift 都不改变 selected/tradable。
- Prediction 仍尽力计算并持久化 `execution_risk_probability`；risk 缺失、高于历史 0.40 或风险模型未认证都不阻断模型 paper。0.35/0.40/0.45 只保留为反事实 shadow sensitivity。
- final certification 改为冻结主模型阈值证据：support、AP>prevalence、ROC-AUC>=0.50、threshold selected>=1、`3TP-FP>0`；execution-risk 不进入 mask，也不再是 generation blocker。
- execution-risk 训练异常被隔离为 `shadow_training_failed:*`，只写审计，不拖垮主模型训练。
- 独立审查额外发现 promotion lifecycle 仍检查 `metrics.execution_risk.certified`，已删除并新增 risk-uncertified 仍可按 current certificate rollover 的回归测试。
- 历史 Phase16 replay 脚本将 0.35/0.40 固化为脚本内 historical constants，保持旧实验可复现，但不重新暴露到当前 decision contract。

### 测试与发布门

- Phase18 审查返工后定向测试：30/30 PASS。
- 全量 pytest：226/226 PASS。
- Python `compileall backend tests scripts artifacts/research`：PASS。
- 前端 `tsc -b && vite build`：PASS，2183 modules transformed。
- `git diff --check`：PASS。
- 高置信 secret scan：219 个 Git 源文件，0 命中。
- SQLite `foreign_key_check`：0 违规；最近 12 个模型工件按生产 import path 12/12 加载成功。

### 真实 Phase18 重训、认证与上线

真实 run：`44ed5ddf-c980-4696-a086-caefb21d39c9`，561 条 frozen final、81 正类、480 负类，generation `eligible=true`、`promoted=1`、blockers=[]。

| Slot | 模型 | Development | Frozen final | 资格 |
|---|---|---:|---:|---|
| model_1 | GradientBoosting `1f800b43` | 67 笔，19TP/48FP，`U=+9` | 4 笔，2TP/2FP，`U=+4` | tradable |
| model_2 | ExtraTrees `0104a7fd` | 67 笔，17TP/50FP，`U=+1` | 3 笔，0TP/3FP，`U=-3` | shadow only |
| model_3 | CatBoost `3f5489a1` | 67 笔，18TP/49FP，`U=+5` | 7 笔，2TP/5FP，`U=+1` | tradable |

- GradientBoosting final AUC≈0.568、AP lift≈+0.044；CatBoost AUC≈0.543、AP lift≈+0.059；二者 `qualified_deployment_evidence=true`。ExtraTrees 虽有正向 ranking evidence，但主模型阈值 final 为负，因此保持 shadow。
- 三个证书都写明 `execution_risk_policy=shadow_only`；risk probability 可用数量为 561，但没有参与任何一笔 final selected。
- 部署一致性验证：三模型 production/evaluation frozen-final 最大概率差均为 0.0；training→final gap=5491 秒，超过 90 分钟标签窗口；decision/cert/fit-scope/age/gap 全部 PASS。
- 2026-08-31 10:12:16（北京时间）完成 staged activation。Prediction 当前 `model_ids=3`、tradable 为 GradientBoosting+CatBoost、ExtraTrees 为 shadow，worker `reason=ok`。
- 激活验收时尚无 10:12:16 之后的新规则准入 sample，因此 production prediction 仍为 0；这是无新输入，不是 worker 停摆，没有向生产数据库伪造样本。

### 热更新与运行验收

- 使用既有 Windows supervisor 自动热加载，`start_runtime.py --reload` 幂等确认 `already_running`，没有启动第二个 Uvicorn。
- `/health=ok`；Collector、Prediction、TrainingWorker、Scheduler、PositionMonitor 均 running。
- ModelHealth 已刷新到新代，状态 `insufficient_data`，原因仅为新代尚无成熟 OOS prediction，不再是 contract stale。
- `rules_only` 持续运行且不受换代阻断；simulation 继续 unlimited-notional。
- `DRY_RUN=true`、`simulation_enabled=true`、`live_trading_enabled=false`。

### 回滚点与技术债

- 未改写历史 position、trade、label、risk 输出或旧模型工件；旧模型仅因版本合同失效。
- 若回滚代码，恢复上一提交并由 supervisor 重启；真实自动 BUY 未被本轮授权。
- scikit-learn `SVC(probability=True)` 仍有 1.11 移除警告，后续应独立迁移到 `CalibratedClassifierCV(SVC(), ensemble=False)`。

### 开发进度

- [x] 恢复生产/Git/模型事实。
- [x] 权威研究与 Phase18 架构裁决。
- [x] 文档冻结与代码实现。
- [x] 独立代码审查与返工 PASS。
- [x] 全量测试、真实 current-contract 认证 PASS。
- [x] 热更新和生产运行验收 PASS。
- [x] commit/push 完成。

## 2026-08-30：Phase17 模型复筛简化、漂移特征治理与无本金模拟

状态：`COMPLETED / DEPLOYED`

### 用户裁决

1. 删除 `<30m +0.04` 固定硬加分。
2. 删除 60–120 分钟一刀切 ABSTAIN；年龄仍可作为普通模型特征，但模型输出后不再二次年龄修正。
3. drift 改为模型更新/特征治理信号：重复漂移特征优先寻找具备业务/数学意义、PIT-safe 且更稳定的组合表达；无合理替代且 OOS 贡献弱时退役。单次 drift 不自动删特征，也不直接冻结 paper。
4. `model_1/2/3/rules_only` simulation 取消 1000 USD 初始本金和现金余额约束；保留名义单笔 sizing、fees、slippage、PnL、ROI、交易数、回撤和退出事实。

### 生产根因与研究证据

- 任务开始时 mature 样本约 2.6k、历史跨度约 13.6 天；`EARLY_STAGE_MODEL` 只是 `<120d` 阶段标签，不是部署 blocker。
- 旧 Active Top3 的决策合同已 stale，因此停止模型评分；同时 rules_only 的有限 1000 USD 账本耗至约 43.61 USD，被 `insufficient_paper_cash` 阻断，形成“新代不激活→baseline 停摆”的耦合。
- feature-governance 只使用最早 2078 条 development 设计，并将最新 520 条冻结为独立保留验证。`price_change_1h` 在连续 development 未来块的 PSI 均值约 0.132；预注册替代 `momentum_accel_1m_vs_5m = ln(1+r_1m)-ln(1+r_5m)/5` 将 development PSI 降至约 0.030，并保留更强 OOS 信息。冻结后保留段 raw 1h PSI≈0.127，替代特征 PSI≈0.044，批准替换。
- `ln(volume_1m+1)`、`volume_1h/swaps_1h` 与 `liquidity/holder_count` 在 development/保留段仍有稳定 OOS 信息，继续保留；只为降低 PSI 构造的 `trade_size_pressure_1m_vs_1h` 因 AP lift≈0.002、信息损失明显而被拒绝。
- 冗余审计确认：`top_bot_degen_percentage` 与 `bot_degen_rate` Spearman=1.0；`ln(price+1)` 与 `ln(marketcap+1)` Spearman≈0.9999；`dexscr_ad`、`dexscr_trending_bar` 在 development 恒为 0。分别保留 `bot_degen_rate`、`ln(marketcap+1)`，其余退出新训练候选池。
- 研究原则采用 NIST AI RMF 的持续监控/维护思路与 scikit-learn 的 chronological split、held-out 泛化评估和相关特征治理原则；PSI 不作为单独删除规则，final holdout 不参与特征设计。

### 实现

- 决策合同升级为 `phase17_model_filter_paper_v1`，认证合同升级为 `phase17_model_filter_certification_v1`。
- 年龄层只保留 Collector/SafetyFilter 的 `2 < age_minutes < 300` 原始准入；`age_admission_only_v1` 的 threshold delta 恒为 0，任一 2–300 分钟子区间均不再 ABSTAIN。
- drift 的 normal/caution/severe 只写健康状态、参与提前重训 cadence 和 feature-governance；不再改变 paper selected，也不再把 execution-risk 从 0.40 收紧到 0.35。
- execution-risk 继续作为本轮未被用户取消的独立固定门，ceiling 为 0.40；其训练仍严格使用 final 前并再留足完整标签窗口的 PIT 样本。
- feature pool 新增统一 materializer `momentum_accel_1m_vs_5m`；旧保存选择中的 `price_change_1h`、`ln(price+1)`、`top_bot_degen_percentage` 自动确定性迁移为新表示。
- simulation 改为 `unlimited_notional`：runtime account 不再保存 `cash_usd/initial_cash_usd`，删除余额检查、买入扣款和退出回款。数据库旧 `initial_cash_usd` 列仅以 0 兼容旧 schema，不再具有资金账户语义。
- `rules_only` 不读取模型 rollover pause，且按 sample 跨 session 全局幂等；模型换代只等待 `model_1/2/3` 平仓并重置模型统计，不再重置或暂停 rules_only。
- 前端移除“当前余额/$1000”展示，改为“持仓名义投入”；`system_status.py` 同步使用真实 `position_monitor_status`，不再误报 paper monitor stopped。
- blocked generation 继续使用 `shadow_model_1/2/3` 保存真正 post-training 预测，不创建仓位、不进入 AdaptivePolicy feedback、不构成 live BUY 授权。

### 独立审查与返工

- 审查覆盖时间泄漏、训练/预测特征一致性、paper/live 边界、rollover 故障恢复、数据库兼容与前端类型。
- 返工修复：shadow risk sensitivity 仍错误排除 severe drift；rules_only direct-open 仍按 session 幂等；drift 只记录却未进入更新 cadence；`system_status.py` 读取过期状态键；admission-only 审计指标因缺失可选年龄列误显示 selected=0；开发文档曾被 Windows 编码链误写为乱码。上述问题均已返工，开发文档已按当前长期合同重建为纯 UTF-8，并删除临时计划。
- 最终确认 final feature ranking 只读取 `final_split.train_indices`，承担 final 认证的 fitted instance 即 production instance，final 标签不进入部署前 estimator fit。

### 专业测试与发布门

- 全量 pytest：`224/224 PASS`。
- Phase17 扩展定向测试：决策、特征迁移、trainer、prediction/paper、rollover、scheduler、model health、deployment certification 全部 PASS。
- Python `compileall backend tests scripts`：PASS。
- 前端 `tsc -b && vite build`：PASS，2183 modules transformed。
- `git diff --check`：PASS。
- secret scan：扫描 7641 个文件，0 命中。
- SQLite foreign key：0 违规；最新 12 个模型工件全部成功加载。
- `/health`：HTTP 200 / `status=ok`；runtime supervisor 已检测代码变更并重启 child。

### 真实 Phase17 重训与生产验收

真实训练 run：`905c3aed-e1a5-4ba8-becb-3949912bf102`，544 条 frozen final，82 正类，正类率 15.07%。新 Top3：

| 模型 | Development | Final 模型阈值 | 固定 risk 0.40 后 | AUC / AP lift |
|---|---:|---:|---:|---:|
| GradientBoosting | 65 笔，18TP/47FP，`U=+7` | 7 笔，3TP/4FP，`U=+5` | 2 笔，0TP/2FP，`U=-2` | 0.558 / +0.045 |
| ExtraTrees | 65 笔，17TP/48FP，`U=+3` | 4 笔，0TP/4FP，`U=-4` | 1 笔，0TP/1FP，`U=-1` | 0.598 / +0.057 |
| TabPFN | 98 笔，25TP/73FP，`U=+2` | 0 笔 | 0 笔 | 0.523 / +0.015 |

- 三个模型均使用 `age_admission_only_v1`，final drift 均为 normal；这次不存在年龄加分、60–120 ABSTAIN 或 drift veto。
- 三个模型 production/evaluation 在 frozen final 上最大概率差均为 0.0；training→final gap 均为 5507 秒，超过 90 分钟标签窗口。
- generation 未激活：唯一 blocker 为 `no_top3_model_has_positive_ranked_end_to_end_final_evidence`。具体说，GradientBoosting 的基础模型 final 为正，但固定 execution-risk 0.40 后留下的 2 笔均为负；本任务没有擅自取消用户未要求删除的 execution-risk，也没有强制上线。
- 最新三模型已进入 shadow：GradientBoosting `7e687878`、ExtraTrees `8c66973b`、TabPFN `18ae4827`。验收时训练完成后尚无新准入样本，因此 shadow 写入数为 0，属于没有 post-training 输入而非 worker 停摆。
- rules_only 已恢复并持续运行：在累计 realized PnL 约 -1874.58 USD 的情况下仍有 487 笔已平仓、1 笔持仓，证明不再受本金耗尽阻断。
- Collector、Prediction、TrainingWorker、Scheduler、PositionMonitor 均 running；PositionMonitor 目标 2 秒、最近周期无 market/network failure；`DRY_RUN=true`、`live_trading_enabled=false`。

### 回滚点与未决技术债

- 未改写历史 position、trade、label 或旧 simulation session 事实；回滚可恢复上一提交并由 supervisor 重启，旧工件仍按版本合同 fail-closed。
- scikit-learn 已提示 `SVC(probability=True)` 将在 1.11 移除，后续应迁移到 `CalibratedClassifierCV(SVC(), ensemble=False)`；本轮未扩大为额外模型重构。
- `EARLY_STAGE_MODEL` 名称仍表示历史不足 120 天，不影响部署逻辑；若需要改善产品语义，可在后续独立任务中改名为 short-history，而不改变统计合同。

### 开发进度

- [x] 恢复现状与根因审计。
- [x] 权威研究、development drift 审计、保留段验证、冗余审计。
- [x] 编码前架构与验收合同冻结。
- [x] Phase17 代码实现。
- [x] 独立代码审查与返工 PASS。
- [x] targeted/full tests PASS。
- [x] 生产热更新、真实重训与运行验收 PASS。
- [x] commit/push 完成。

## 2026-08-27：模型零买入根因修复与文档治理

状态：`CLOSED / SUPERSEDED BY PHASE17`

### 需求

1. 将需求分析、量化根因、权威研究、架构设计、性能评估、开发、审查、测试、上线、文档收口、commit/push 固化为永久工作流。
2. README 只保留稳定说明；开发进度迁入本文。
3. 修复最新三模型零买入背后的近期泛化与部署认证问题，不以降低风控制造交易。
4. 完成热更新和可审计交付。

### 根因证据

- 最新 Active Top3 为 CatBoost、LightGBM、GradientBoosting。
- 上线后每个模型完成约 30 次评分，均未产生最终 selected。
- 拒绝主要来自校准概率低于阈值，少量来自年龄 abstain、execution-risk 和 drift。
- 即使移除附加策略门，CatBoost 仍无基础 budget 候选，另两模型各只有极少候选。
- 上线前 final 353 条窗口中：CatBoost AUC≈0.500、AP≈0.146，基础 budget 3 笔全错；LightGBM AUC≈0.443、AP≈0.120，基础 budget 12 笔全错；GradientBoosting AUC≈0.461、AP≈0.134，基础 budget 14 笔仅 1 个正类。
- 当时认证合同只要求 Top3 中任一模型完整策略 selected≥1；负效用只有 selected≥8 才阻断。CatBoost 与 GradientBoosting 都以 1 笔、0 胜 1 负通过 generation 级 smoke，构成明确放行漏洞。
- 同期 rules_only 全买表现显著为负，说明近期市场本身差，不能用取消风控替代模型修复。
- 理论触发 1.8x 的个别样本在 Jupiter 可执行报价后仍亏损，说明标签代理与真实可执行收益仍需持续分开监控。

### 研究与方案依据

采用以下已认可原则，并在项目语境中做保守落地：

- 时间序列使用 chronological split 与 gap，最终时间留出不参与选择；
- 校准、模型选择和部署认证数据职责分离；
- final holdout 只提供部署 veto，不重新选择 Top3；
- 排序能力同时看 ROC AUC 和 Average Precision 相对正类率；
- 稀疏决策使用显式经济盈亏平衡和最小证据门；
- 漂移、执行风险与模型分数分层治理，不能用单一阈值混合；
- 旧工件合同不匹配时 fail-closed，Collector 和持仓退出继续运行。

主要来源：scikit-learn 官方时间序列切分、概率校准和模型评价文档；Angelopoulos 等《Conformal Risk Control》（ICLR 2024）；Bailey 等关于回测过拟合概率的研究；NIST AI RMF 与美联储 SR 11-7 的模型验证和持续监控原则。

### 冻结架构方案

- 每个 Top3 分别记录样本量、AUC/AP、基础 budget、完整 age+risk+drift 策略与部署证书；
- final 只做 deployment veto，不改变 Top3、特征、阈值、年龄策略或 risk ceiling；
- generation 至少要有一个“同一模型同时具备正向排序证据与正向完整策略证据”的 qualified model 才可 staged activation；激活后的任一未逐模型 qualified slot 仍不得开仓，只允许持续 shadow 观测；
- 任一候选只有自身 `qualified_deployment_evidence=true` 才能成为 tradable model；severe drift、execution-risk 未认证及 generation 无任何 qualified model 都会阻断整代；
- 决策/认证合同升级时旧工件自动 fail-closed，Collector、rules_only、标签和已有仓位退出不受影响；
- 不把近期漏单结果用于即时放松 `<30m +0.04` 或 risk 0.40/0.35；年龄策略变化必须只从 development OOS 预注册 challenger 中选择并再经 final veto；
- daily recovery 使用新增样本重新训练，禁止手改 Active slots。

### 2026-08-28 第二轮审计与优化扩展

新增生产证据：

- 2026-08-27 训练出的 TabPFN/CatBoost/RBF-SVM 从未正式上线：其 final 基础预算分别为 0/4、1/7、0/3 正类命中，固定 3:1 经济单位分别为 -4/-3/-3；当前 0 模型交易的直接原因是旧 v3 Active Top3 已按新合同停牌，而新一代认证失败。
- 训练完成后真正未见过的成熟样本中，年轻 Token 总池仍显著亏损，支持继续保留 `<30m +0.04`；但 `60–120m` 在近期两个独立窗口转正，只构成 challenger 信号，不能据此人工解禁。
- execution-risk 在 post-training 干净样本上的 AUC 约 0.62，说明有排序信息但不足以把 0.35 当作高精度分类边界；本轮继续保留 0.35/0.40，只增强分层 outcome monitoring。
- 发现新的部署一致性漏洞：当前 `evaluation_bundle` 只训练到 final 前并承担认证，但 production bundle 随后又在包含 final holdout 的 `refit_indices` 上重拟合。冻结 final 上 CatBoost/RBF 的 evaluation AUC 约 0.562/0.563，而含 final 标签的 production bundle 可虚高到约 0.913/0.955，证明拿证实例与待部署实例并不一致。

第二轮冻结实现：

- [ ] 部署工件改为“认证实例即部署实例”，final holdout 永不进入待部署 estimator fit；升级决策/认证 provenance，使旧工件自动 fail-closed。
- [ ] 年龄策略只预注册 `strict` 与 `reopen_60_120` 两个 challenger；每模型只用 chronological development OOS 选择，平局/无正效用时保留 `strict`，final 只能否决。
- [ ] 无当前可交易模型时，对最新 blocked generation 只加载三个 candidate，使用 `shadow_model_1/2/3` 记录 post-training 预测；shadow 永不进入持仓 reconcile、AdaptivePolicy 或 live BUY。
- [ ] 增加 age/risk/shadow outcome 监控与 risk 0.35/0.40/0.45 灵敏度审计，不以该审计直接改生产上限。
- [ ] 修复过期 `phase16_gate_diagnostic.py`，补实例一致性、年龄策略隔离、shadow 幂等/不下单等回归测试。

### 开发进度

- [x] 永久工作流与文档职责写入《开发文档.md》。
- [x] README 重构为稳定系统说明。
- [x] 有效历史迁入本文。
- [x] candidate-level deployment certification 模块落地。
- [x] Training generation gate 改为 Top3 全部逐个通过。
- [x] 决策合同升级到 v4，旧 v3 工件 fail-closed。
- [x] 认证单元测试补齐。
- [ ] 代码审查 PASS。
- [ ] targeted/full tests PASS。
- [ ] 生产热更新与运行验收 PASS。
- [ ] commit/push 完成。

### 回滚点

代码变更前的 Git 工作树和生产数据库保持不删除；本任务不修改标签、schema、资金或历史成交。若热更新失败，恢复上一代码提交并重启；Active v3 工件和原始成交事实不被改写。

## 历史有效记录（由 README/开发日记迁入）

### 2026-08-25：Phase16 v5 与策略快照

- 当前标签迁移到 0.9/1.8/90min v5，迁移使用完整 K 线覆盖和数据库备份。
- position 保存退出策略快照，旧仓继续按旧 1.6x/60min，新仓使用 1.8x/90min。
- development OOS sigmoid 校准、model-specific sparse budget、age gate、execution-risk 和 drift gate 上线。
- 训练、迁移期间 Collector 保持运行；旧工件合同不匹配时模型入口 fail-closed。

### 2026-08-25：GMGN 429 与公共只读 fallback

- 多 Key 不能放大共享 IP 吞吐；429 cooldown 按 slot 隔离。
- Collector 使用持久 worker circuit breaker 和有限 half-open；重启不清除退避。
- Key 按 discovery 与 auxiliary 角色隔离，生产总速率保守控制。
- simulation 持仓价格使用允许的 DexScreener 只读 fallback；live 坚持 GMGN-only。
- simulation fee-time SOL/USD 使用 Coinbase Public 主路径、GMGN fallback。
- fallback 不参与 Collector 准入、模型特征或标签，不补造训练事实。

### 历史数据合同迁移

- `completed` 生命周期退出当前采样、训练和交易；
- 标签从旧 H2/H1 逐步迁移到当前 v5，legacy 字段只保留 provenance；
- age、Top10、成交均额等准入口径已统一到 Collector、SafetyFilter、CSV importer 与 legacy 路径；
- 迁移均要求备份、行数、标签域、FK 和违规数验证；
- 事故或异常收益保留原始事实，通过明确 `performance_excluded` provenance 排除，不篡改账本历史。
