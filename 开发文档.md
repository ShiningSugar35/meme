# Solana Meme Quant Trading System：开发与架构规范

> 适用目录：`D:\meme`
> 当前分支：`main`
> 部署形态：Windows 本地单机、SQLite、CPU-first
> 当前安全状态：`DRY_RUN=true`，真实自动 BUY 未授权
> 文档职责：本文是长期研发流程、架构、算法合同与发布门的最高工程依据；稳定使用说明见 `README.md`，历史实施与验收证据见 `进度验收.md`。

## 1. 权威顺序与强制研发流程

### 1.1 权威顺序

发生冲突时，按以下顺序裁决：

1. 用户在当前任务中最后明确确认的要求；
2. 本文的安全边界、现行架构合同和验收标准；
3. `README.md` 的稳定产品边界；
4. `进度验收.md` 中最近一次已完成任务的实施与运行事实；
5. 当前代码、数据库和模型工件事实；
6. 历史研究报告、旧设计稿和旧提交。

不得让旧研究结论覆盖用户的新裁决；也不得以“优化”为名，暗改标签、准入阈值、资金语义、模型晋级、退出策略或实盘权限。

### 1.2 每次任务必须完成的流程

任何开发、修复、策略调整、部署或文档任务，必须依次完成：

1. 读取 `AGENTS.md`、本文、`README.md`、`进度验收.md`；
2. 恢复 Git、数据库、运行态、模型与未提交变更事实；
3. 明确需求边界、禁止事项和可回滚点；
4. 量化定位根因，不根据界面状态或单个样本猜测；
5. 查阅权威文档或论文，说明哪些原则适用、哪些不适用；
6. 评估架构、资源、复杂度、时间泄漏和兼容风险；
7. 在本文或专项设计稿中先冻结方案与可执行验收标准；
8. 开发并同步更新 `进度验收.md`；
9. 独立审查完整 diff，发现问题必须返工；
10. 执行定向测试、全量测试、编译、前端构建、数据库和运行验收；
11. 安全热更新，验证 API、worker、数据库与前端；
12. 删除本文中的临时任务计划，收口文档；
13. commit、push，并确认没有本任务未解释残留。

不得因改动看似较小而跳过审查、测试、上线验证或文档收口。

## 2. 不可违反的安全与数据边界

### 2.1 交易安全

- 默认并要求保持 `DRY_RUN=true`；真实自动 BUY 只有在用户明确授权且全部实盘门通过后才能启用。
- 模拟器不得加载私钥、签名或广播交易；Jupiter 仅允许 Token→USDC quote-only 路由验证。
- `trading/live` 是唯一允许承载真实签名/广播逻辑的边界；未知提交状态不得重放订单。
- Collector、`rules_only`、已有仓位退出、标签成熟和审计账本不得被模型训练、认证失败、drift 或换代流程阻断。

### 2.2 数据与模型安全

- 禁止随机时间切分、未来字段、入场后 K 线、标签、退出结果、PnL 或交易状态进入模型输入。
- final holdout 只作部署否决；不得反向选择 Top 3、特征、阈值、概率校准器或风险上限。
- 拿 final 证书的 fitted estimator 必须就是唯一可部署 estimator；final 标签不得用于部署前 refit。
- 不覆盖 `.env`、本地数据库、模型工件或用户未提交变更；任何 schema/数据迁移必须先备份并验证回滚。
- 同一生产 SQLite 只能由一个 FastAPI runtime 写入；不得并行启动第二个 Uvicorn。
- 秘密不得写入前端 bundle、数据库业务字段、异常响应、日志快照、测试资产或 Git。

## 3. 现行系统合同与运行拓扑

### 3.1 版本合同

- 特征代：`event1m_regime_v3`
- 标签：`sl090_tp180_m90_binary_v5`
- Paper 决策合同：`phase19_expected_return_paper_v1`
- 部署认证合同：`phase19_expected_return_certification_v1`
- 经济目标：`precision_recall_payoff_v4`
- 年龄策略：`age_admission_only_v1`
- 新仓退出快照：`m90_tp180_sl090_v2`

任何上述合同变更必须升级版本。旧工件在新代码下自动 fail-closed，但历史事实不得删除或改写。

### 3.2 主链

```text
GMGN Trenches 候选发现
  → 本地粗筛与权威 enrichment
  → fail-closed 安全准入
  → 不可变入场时 PIT 特征快照
  → rules_only 持续基线模拟
  → Active Top 3 校准概率与模型专属阈值
  → model_1 / model_2 / model_3 名义模拟；execution-risk 仅 shadow 记录
  → 0.9x / 1.8x / 90 分钟退出与 first-touch 标签
  → chronological development OOS 训练
  → frozen final 部署否决
  → 合格模型平滑换代；不合格模型 shadow
```

年龄、execution-risk 和 drift 都不在模型输出后再次投票：年龄仅作为原始准入事实和可选普通特征，execution-risk 仅用于 shadow outcome/研究，drift 仅用于监控、重训和特征治理。

### 3.3 Runtime 组件

- Collector：发现、enrichment、规则准入、样本落库和标签成熟；
- Prediction：当前合同模型或 blocked generation shadow 评分；
- PositionMonitor：默认目标 cadence 2 秒，管理模拟和受控实盘退出；
- TrainingWorker：串行消费持久训练任务；
- Scheduler：北京时间训练计划、换代暂停和恢复；
- ModelHealth：成熟 OOS 表现与 drift 健康监控；
- Reconciliation/Liquidation：幂等订单恢复和清仓工作流；
- React/Vite：只展示后端持久事实，不在前端复制领域规则。

所有时间以 UTC 持久化，页面按 `Asia/Shanghai` 展示。

### 3.4 EARLY_STAGE 语义

`EARLY_STAGE_MODEL` 仅表示可用历史跨度不足 120 天，并切换相应时间切分口径；它不是部署 blocker，也不表示训练未完成。部署资格只由当前版本的数据、拟合范围和 final 证书决定。

## 4. 生产准入与标签合同

### 4.1 发现和生命周期

仅处理允许清单内的 Solana Launchpad，生命周期只允许：

- `new_creation`
- `near_completion`

`completed` 永久退出当前采样、训练和新开仓。Discovery 只负责传输和候选范围，业务阈值由本地代码统一执行。

### 4.2 核心准入

候选必须同时满足本地 SafetyFilter 合同，包括但不限于：

- `rug_ratio < 0.2`
- `insider_ratio < 0.2`
- `bundler_rate < 0.2`
- `liquidity > 4800 USD`
- 严格 `0.14 < top_10_holder_rate < 0.25`
- `fresh_wallet_rate < 0.2`
- mint/freeze 权限已放弃，burn 状态明确
- `rat_trader_amount_rate < 0.2`
- `29 < holder_count < 1000`
- `marketcap > 5000 USD`
- `sell_tax < 0.025`、`buy_tax < 0.025`
- `sniper_count < 10`
- 严格 `2 < age_minutes < 300`
- `liquidity / holder_count > 50`
- `swaps_1h > 19`
- `volume_1h / swaps_1h > 31`
- `(0.5 + smart_degen_count + renowned_count) * volume > 5000`
- top-holder 结构满足冻结合同

关键字段缺失、不可解析、NaN/Inf、非法负数或状态未知时 fail-closed，不用默认值补造安全事实。

### 4.3 样本与标签

每次通过准入的观测按 `(chain, address, observed_at)` 形成独立不可变样本；同一 Token 在前一策略窗口结束后再次出现，可以形成新样本。

当前标签采用 90 分钟 first-touch：

- 先触及 0.90x：`tag=0`；
- 先触及 1.80x：`tag=1`；
- 同一分钟同时触发：保守按止损优先；
- 90 分钟内未先触及 1.80x：`tag=0`。

标签代理收益、可执行模拟 PnL 和未来真实链上 PnL必须分开核算。

## 5. 特征合同与 drift 治理

### 5.1 PIT allowlist

模型只使用显式 allowlist。地址、名称、身份字段、`launchpad`、标签、未来 K 线、first-touch、仓位、退出、成交和 PnL 永久排除。

- raw `entry_price`、`age_minutes`、liquidity 作为准入或会计事实保存；
- 年龄可使用 `ln(age+1)` 作为普通模型特征，但输出端不再二次加权；
- raw liquidity 仍用于名义 sizing 与成交安全；模型优先使用有业务尺度的相对表达，如 `liquidity/holder_count`；
- 当前新训练可选目录 39 个，默认候选 29 个；完整清单以 `backend/app/ml/features.py` 为唯一代码事实。

### 5.2 当前治理裁决

- `price_change_1h` 退役；使用 `momentum_accel_1m_vs_5m = ln(1+r_1m) - ln(1+r_5m)/5` 表示 1 分钟动量相对 5 分钟平均动量的加速度。
- `ln(price+1)` 与 `ln(marketcap+1)` 近乎完全秩相关；新训练保留业务含义更明确的 market cap，绝对 price 仅保留旧工件兼容。
- `top_bot_degen_percentage` 与 `bot_degen_rate` 完全重复；保留后者。
- `dexscr_ad`、`dexscr_trending_bar` 在当前 development 恒为 0，退出新训练候选池。
- `ln(liquidity_usd)` 仅保留审计，不进入主模型或 execution-risk 新训练输入。
- `ln(volume_1m+1)`、`volume_1h/swaps_1h`、`liquidity/holder_count` 因仍有稳定 OOS 信息而保留。

所有组合特征必须只使用入场时已经可得的事实，并且训练与 inference 通过同一 materializer 生成。

### 5.3 drift 不是自动删除规则

Drift 的 normal/caution/severe 是模型更新和 feature-governance 信号，不直接拒绝 paper 交易。治理顺序为：

1. 在 chronological development 多个未来块确认是否重复漂移；
2. 同时检查覆盖率、OOS 排序信息、经济贡献、相关簇和业务含义；
3. 优先尝试 PIT-safe、数学和业务含义明确的尺度化、比率、差分或速度/加速度表达；
4. 只有替代表达稳定性改善且 OOS 信息不下降时才替换；
5. 若重复漂移、OOS 贡献弱且无合理替代，才从候选池退役；
6. 单次 PSI、单次模型重要性或 final holdout 结果不得单独决定删特征。

强相关候选按相关簇保留代表项；不得为了降低 PSI 构造无业务含义、明显损失信息的组合。

## 6. 训练、校准、选择与部署认证

### 6.1 数据门和时间切分

当前 mature 二分类样本少于 1000 时，自动训练不排队，Collector、标签和 `rules_only` 继续运行。达到门槛后：

- 禁止随机切分；
- development 使用 expanding chronological folds；
- 每个 train/test、calibration 和 final 边界至少保留当前标签窗口，即 90 分钟 gap；
- 每 fold 的特征排序只读取该 fold train；
- final holdout 永不参与特征、模型、阈值、校准或 Top 3 选择。

### 6.2 概率校准和期望收益 operating point

- 每模型用 development OOS raw score 拟合 sigmoid/Platt calibrator；小样本不使用 isotonic；
- 当前 Winner/Loser 代理收益比为 `+3/-1`。设 Precision=`P`、Recall=`R`，唯一主目标为 `J(P,R)=R(4-1/P)=(3TP-FP)/N_positive`；
- 盈亏平衡 Precision 为 `1/(3+1)=25%`，这是期望收益的数学边界，不是共享概率阈值；不得把 calibrated probability 硬裁到 0.25；
- production threshold 在所有 distinct development-OOS calibrated probability operating points 中搜索；`min_trades` 只作最低统计样本约束，不再限制 5%/7.5%/10%/15% 固定预算；
- 选择 `J>0` 且 J 最大的 operating point；若 J 数值并列，依次优先 Recall、Precision、Wilson 下界更高者。

### 6.3 Top 3

候选覆盖线性、树、boosting、bagging、kernel、foundation/AutoML 家族。Top 3 只使用 development OOS 的同一个期望收益目标：

```text
b = 3
P_break_even = 1 / (b + 1) = 0.25
J = Recall * ((b + 1) - 1 / Precision)
  = (3 * TP - FP) / N_positive
```

- `economic_score`、`ranking_economic_score`、`composite_score` 在当前合同下均表示 development OOS J，不再存在 `0.60E+0.40G` 的复合排名；
- feature-count 奥卡姆规则仍在同一个 J 上使用 one-standard-error 与不超过 8% 的相对 J 损失约束，避免用额外目标补分；
- Top 3 直接取 J 最高的三个候选；J 并列时优先 Recall、Precision，再考虑特征数与复杂度；
- AP/AP skill、ROC-AUC、时间稳定性、衰减、execution PnL 与模型家族多样性全部仅作审计，不得创造独立排名分或牺牲更高 J；
- final 结果不能换入非 Top 3 候选或重排。

### 6.4 execution-risk

execution-risk head 估计入场后若触发止损时发生 severe gap 的概率：

- 只使用 entry-time PIT 特征；
- 训练样本必须位于 final start 之前，并再向前留出完整 90 分钟标签隔离；
- 内部训练、校准、验证按时间顺序分离；
- final 或之后的退出结果不得进入风险头；
- risk probability 继续随 prediction 持久化，并保留 0.35/0.40/0.45 等历史阈值的反事实灵敏度报表；
- execution-risk 不参与 paper selected/tradable、deployment certification 或 generation activation；
- 风险模型不可用、未认证或训练异常时，仅记录 shadow unavailable，不得阻断主模型训练、认证或 paper。

若未来重新引入 execution-risk 到交易决策，必须另走 development OOS、独立 final 与版本化合同，证明其在明确经济效用上持续改善；不得从当前 final 事后选择新阈值。

### 6.5 final 部署否决

新一代 Top 3 在 development 全部冻结后，final 只执行 veto。每个模型记录 final prevalence、AP、AP lift、ROC-AUC，以及冻结模型阈值本身的 Precision、Recall、TP/FP、`3TP-FP` 和 `J=(3TP-FP)/N_positive`；execution-risk 只作为并行 shadow 字段记录。

“可部署证据模型”必须是同一个模型同时满足：

1. final 样本、正类、负类达到最低证据门；
2. 冻结模型阈值至少选中 1 条；
3. final `J > 0`；
4. production/evaluation 对 frozen final 的概率在数值容差内逐行一致；
5. production training end 早于 final start 且保留完整标签 gap。

AP、AP lift、ROC-AUC 在 final 继续作为排序能力诊断，但不作为 paper deployment blocker，也不得触发重排或阈值回写。某一 Top3 slot final J 非正只使该 slot 保持 shadow，不拖死其他合格 slot；整代无任何合格证据模型时才阻断 generation。execution-risk 和 drift 只记录监控/研究状态。

整代至少有一个合格证据模型才允许 staged activation；激活后仍只有自身 `qualified_deployment_evidence=true` 的 slot 可以开仓，其他 slot 只做 shadow。

### 6.6 Shadow 和换代

- 当前 slot 未获逐模型证书，或整代 certification blocked 时，可用 `shadow_model_1/2/3` 持续记录真正 post-training 概率、risk、drift 和成熟 outcome；
- shadow 不创建仓位、不进入 AdaptivePolicy feedback、不构成 live BUY 授权；
- 在线最多加载三个模型和一个 execution-risk head；
- 新代激活只等待 `model_1/2/3` 平仓，不等待、不暂停、不重置 `rules_only`；
- 模型切换只重置三个模型策略的代际统计，不创建新的有限本金账户。

## 7. Paper 决策与无本金名义模拟

### 7.1 模型决策

Paper 主链固定为：

```text
规则安全准入
  → calibrated probability >= model development OOS threshold
  → model_1 / model_2 / model_3 simulation
```

- 任一 `2 < age_minutes < 300` 样本均不因年龄额外提高阈值或自动 ABSTAIN；
- execution-risk 和 drift 都不改变 selected，也不冻结 paper；risk probability 仅作为 shadow/研究字段；
- AdaptivePolicy 只能在模型基础阈值之上收紧，不能下调 development OOS 冻结阈值。

### 7.2 四策略与会计

可执行四策略：`model_1`、`model_2`、`model_3`、`rules_only`。

Simulation 使用 `unlimited_notional`：

- 不设置初始本金、可用现金或余额不足门；
- 累计亏损不会停止任何策略继续模拟；
- 每笔仍用 `min(1% × entry_liquidity_usd, 50 USD)` 作为可比名义投入；
- 名义投入不从有限余额扣减；
- 继续完整记录 position、平台费、网络费、滑点、执行偏差、毛/净 PnL、ROI、交易数、胜率、Precision、Recall 和回撤；
- 每策略最大同时持仓 10 个。

`rules_only` 对每个通过准入的 sample 全局幂等地建立基线，不经过模型、risk、drift、认证或换代门；它用于评价准入池和执行质量，不是推荐全买策略。

### 7.3 退出与市场数据

- 新仓：0.90x 止损、1.80x 止盈、90 分钟到期；
- 旧仓继续按自身快照退出，不受新配置重解释；
- 触发退出后冻结 reference、trigger time 和 reason；重启只续原退出链；
- simulation current price 默认 DexScreener Public-first，失败才 GMGN；live 坚持 GMGN-only；
- 模拟手续费的 SOL/USD 默认 Coinbase Public 1m closed candle-first，GMGN fallback；
- no-route 与 provider unavailable 必须分流，provider unavailable 不得伪装为 rug 或全损。

## 8. 调度、资源与可靠性

### 8.1 训练调度

- 北京时间默认 16:00 暂停三个模型新开仓，17:00 训练；
- `insufficient_data`、Active Top 3 未就绪、合同 stale、部署认证 blocked 或 `drift_detected` 时使用每日恢复训练；
- 其他健康状态按周训练；
- 启动补跑和计划点必须幂等；
- 训练失败有限重试，不得形成无界任务风暴。

### 8.2 资源预算

- 在线 Prediction：最多三个小型表格模型 + 一个 execution-risk head；
- 禁止新增常驻深度模型；
- PositionMonitor 目标 2 秒，同 Token 合并行情请求，避免 head-of-line blocking；
- 单次训练可使用较高 CPU，但必须由 durable worker 串行执行，候选内部限制线程；
- 部署认证复杂度限制为 `O(Top3 × N_final)` 时间和 `O(N_final)` 瞬时内存；
- SQLite 事务保持短小，研究和训练不得长时间持有写锁。

### 8.3 运行与热更新

常用入口：

```powershell
D:\meme\.venv\Scripts\python.exe D:\meme\scripts\start_runtime.py --reload
D:\meme\.venv\Scripts\python.exe D:\meme\scripts\system_status.py
D:\meme\.venv\Scripts\python.exe D:\meme\scripts\training_status.py
```

热更新前确认 supervisor PID 和当前监听者；不得另起第二个 Uvicorn。发布后必须同时检查 `/health`、Collector、Prediction、TrainingWorker、Scheduler、PositionMonitor、数据库、模型工件和前端，而不是只看进程存在。

## 9. 测试与发布硬门

当前非实盘版本发布至少满足：

- 相关定向测试和全量 pytest 全过；
- Python compile、前端 production build、`git diff --check` 全过；
- final holdout 未参与特征、模型、阈值或校准选择的回归测试；
- deployment certification 的正例、负例、无信号、单笔亏损、排序反转和 risk unavailable 回归；
- 年龄不加分、不 ABSTAIN，drift 不阻断 paper，无本金余额不阻断 simulation 的专项回归；
- 旧合同工件不产生模型新仓，`rules_only` 仍持续；
- shadow 不下单、不污染 AdaptivePolicy、重启幂等；
- 三个新候选工件可加载，SQLite foreign key 0 违规；
- secret scan 0 命中；
- `/health=ok`，所有核心 worker 状态可解释；
- `DRY_RUN=true`、live BUY 未武装；
- README 只保留稳定说明，本文无已完成临时计划，进度证据写入 `进度验收.md`；
- commit 与 push 成功。

任一硬门失败必须返工，不得以“仅模拟盘”为由跳过。

## 10. 研究依据

- scikit-learn，TimeSeriesSplit：`https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.TimeSeriesSplit.html`
- scikit-learn，Cross-validation：`https://scikit-learn.org/stable/modules/cross_validation.html`
- scikit-learn，Probability calibration：`https://scikit-learn.org/stable/modules/calibration.html`
- scikit-learn，Permutation importance：`https://scikit-learn.org/stable/modules/permutation_importance.html`
- scikit-learn，Correlated feature importance：`https://scikit-learn.org/stable/auto_examples/inspection/plot_permutation_importance_multicollinear.html`
- scikit-learn，Decision threshold tuning：`https://scikit-learn.org/stable/modules/classification_threshold.html`
- NIST AI RMF：`https://www.nist.gov/itl/ai-risk-management-framework`
- NIST AI RMF Playbook：`https://airc.nist.gov/airmf-resources/playbook/`
- Bailey et al.，The Probability of Backtest Overfitting：`https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2326253`
- Han, Huang & Wang，Model Assessment and Selection under Temporal Distribution Shift，ICML 2024：`https://proceedings.mlr.press/v235/han24b.html`
- Angelopoulos et al.，Conformal Risk Control，ICLR 2024：`https://openreview.net/forum?id=33XGfHLtZg`
