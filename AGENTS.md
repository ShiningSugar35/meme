# 项目接手与研发强制规范

任何开发、修复、策略调整、部署或文档任务开始前，必须依次阅读：

1. `开发文档.md`：研发流程、现行架构、算法合同、安全边界与验收标准的最高工程依据。
2. `README.md`：稳定产品边界、当前业务规则、启动与使用说明。
3. `进度验收.md`：生产基线、历史裁决、当前任务进度、审查与上线证据。

所有非只读任务统一采用 5 阶段闭环：**范围/根因与必要研究 → 架构与实施方案 → 实现 + defect-first Review + 分级验证 → 按风险发布/上线 → 文档归档 + 临时文件清理 + commit/push**。不得跳过与风险相匹配的审查和验证，但也不得为纯文档/规范改动机械执行全量训练、全仓测试、前端构建或热更新；具体分级规则以 `开发文档.md` 为准。

量化与交易安全边界：

- 禁止随机切分、时间泄漏、final holdout 反向选模型/特征/阈值、入场后事实进入模型输入。
- final holdout 仅作部署否决；只有取得当前版本逐模型部署证书的模型允许新开仓，未合格模型只可影子评分。
- 不得仅为增加交易频率降低概率、年龄、执行风险、漂移或实盘安全门。
- 保持 `DRY_RUN=true` 和实盘自动 BUY 停放，除非用户明确授权且全部实盘门禁通过。
- Collector、`rules_only`、已有仓位退出与审计账本不得被模型训练、认证失败或换代流程阻断。
- 不覆盖 `.env`、本地数据库、模型工件或用户未提交改动；任何迁移先备份并验证可回滚。

文档职责必须保持清晰：README 不记录开发流水；开发文档不长期保留已完成的临时任务计划；进度、审查、测试、上线和回滚证据统一写入 `进度验收.md`。
